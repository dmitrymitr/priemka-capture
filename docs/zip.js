/* ZIP store, UTF-8 names, CRC32; files remain Blobs to avoid one giant ArrayBuffer. */
const crcTable = Array.from({length:256},(_,n)=>{for(let k=0;k<8;k++) n=n&1?0xedb88320^(n>>>1):n>>>1;return n>>>0;});
async function crc32(blob){let crc=0xffffffff;const reader=blob.stream().getReader();for(;;){const {done,value}=await reader.read();if(done)break;for(const b of value)crc=crcTable[(crc^b)&255]^(crc>>>8);}return (crc^0xffffffff)>>>0;}
async function makeZip(files){const body=[],central=[];let offset=0,centralSize=0;for(const f of files){const name=new TextEncoder().encode(f.name),blob=f.blob,size=blob.size,crc=await crc32(blob);if(size>0xffffffff||offset+size>0xffffffff)throw Error('Архив слишком большой. Выгрузи меньшую часть.');const a=new Uint8Array(30+name.length),v=new DataView(a.buffer);v.setUint32(0,0x04034b50,true);v.setUint16(4,20,true);v.setUint16(6,0x800,true);v.setUint16(12,33,true);v.setUint32(14,crc,true);v.setUint32(18,size,true);v.setUint32(22,size,true);v.setUint16(26,name.length,true);a.set(name,30);body.push(a,blob);const c=new Uint8Array(46+name.length),w=new DataView(c.buffer);w.setUint32(0,0x02014b50,true);w.setUint16(4,20,true);w.setUint16(6,20,true);w.setUint16(8,0x800,true);w.setUint16(14,33,true);w.setUint32(16,crc,true);w.setUint32(20,size,true);w.setUint32(24,size,true);w.setUint16(28,name.length,true);w.setUint32(42,offset,true);c.set(name,46);central.push(c);centralSize+=c.length;offset+=a.length+size;}const end=new Uint8Array(22),e=new DataView(end.buffer);e.setUint32(0,0x06054b50,true);e.setUint16(8,files.length,true);e.setUint16(10,files.length,true);e.setUint32(12,centralSize,true);e.setUint32(16,offset,true);return new Blob([...body,...central,end],{type:'application/zip'});}
